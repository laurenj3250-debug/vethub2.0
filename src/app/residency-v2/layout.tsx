// Minimal layout for residency-v2 - no QuickPatientNav or global components
export default function ResidencyV2Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
